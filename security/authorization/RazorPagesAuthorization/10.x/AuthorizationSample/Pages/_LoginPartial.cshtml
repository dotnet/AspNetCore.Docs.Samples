@if (User?.Identity?.IsAuthenticated == true)
{
    <form asp-controller="Account" asp-action="Logout" method="post" id="logoutForm">
        <button class="link-style-button" type="submit">Log out</button>
    </form>
}
else
{
    <a asp-page="/Account/Login">Log in</a>
}
